{
  "layers": 4,
  "d_model": 512,
  "n_heads": 8,
  "n_vocab": 50400,
  "norm": "layernorm",
  "pe": "rotary",
  "pe_rotary_dims": 64,

  "seq": 2048,
  "cores_per_replica": 1,
  "per_replica_batch": 8,
  "gradient_accumulation_steps": 16,

  "warmup_steps": 3000,
  "anneal_steps": 27000,
  "total_steps": 1000,
  "lr": 2e-5,
  "end_lr": 4e-6,
  "weight_decay": 0.1,
  "dropout_rate": 0.1,
  "tpu_size": 8,

  "train_set": "state_only.train.index",
  "val_set": {
	  "pisa_eval": "state_only.val.index"
  },

  "val_batches": 100,
  "val_every": 500,
  "ckpt_every": 1000,
  "keep_every": 10000
}
